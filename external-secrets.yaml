# ExternalSecret - syncs database credentials to eso-demo namespace
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: database-credentials
  namespace: eso-demo
spec:
  refreshInterval: 1h  # Check for updates every hour
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes-secret-store
  target:
    name: database-credentials  # Name of the created secret
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/part-of: eso-demo
          synced-by: external-secrets
  data:
  - secretKey: DB_USERNAME
    remoteRef:
      key: database-credentials
      property: username
  - secretKey: DB_PASSWORD
    remoteRef:
      key: database-credentials
      property: password
  - secretKey: DB_HOST
    remoteRef:
      key: database-credentials
      property: host
  - secretKey: DB_PORT
    remoteRef:
      key: database-credentials
      property: port
---
# ExternalSecret - syncs API keys
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: api-keys
  namespace: eso-demo
spec:
  refreshInterval: 30m
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes-secret-store
  target:
    name: api-keys
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: api-keys  # Sync all keys from the source secret
---
# ExternalSecret - syncs TLS certificates
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tls-certificates
  namespace: eso-demo
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes-secret-store
  target:
    name: app-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  data:
  - secretKey: tls.crt
    remoteRef:
      key: tls-certificates
      property: tls.crt
  - secretKey: tls.key
    remoteRef:
      key: tls-certificates
      property: tls.key

